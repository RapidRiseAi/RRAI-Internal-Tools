import { AppShell } from "@/components/app-shell";
import { TaskForm } from "@/components/forms";
import { Card, PageHeader, StatusBadge } from "@/components/ui";
import { prisma } from "@/lib/db";
import { dateShort } from "@/lib/format";
export default async function TasksPage(){ const [tasks,users,projects]=await Promise.all([prisma.task.findMany({include:{assignedTo:true,project:true},orderBy:[{status:"asc"},{dueDate:"asc"}]}),prisma.user.findMany({where:{status:"ACTIVE"},select:{id:true,name:true}}),prisma.project.findMany({select:{id:true,name:true}})]); return <AppShell><PageHeader eyebrow="Workload" title="Tasks" description="Cross-business task system linked to projects and employees, with status, priority and due dates."/><Card><TaskForm users={users} projects={projects}/></Card><div className="mt-6 overflow-hidden rounded-2xl border border-white/10"><table className="w-full bg-white/[0.035] text-sm"><thead className="bg-white/[0.06] text-left text-xs uppercase tracking-wider text-slate-400"><tr><th className="p-4">Task</th><th>Status</th><th>Priority</th><th>Assignee</th><th>Project</th><th>Due</th></tr></thead><tbody>{tasks.map(t=><tr key={t.id} className="border-t border-white/10"><td className="p-4 font-semibold text-white">{t.title}</td><td><StatusBadge value={t.status}/></td><td><StatusBadge value={t.priority}/></td><td>{t.assignedTo?.name ?? "Unassigned"}</td><td>{t.project?.name ?? "—"}</td><td>{dateShort(t.dueDate)}</td></tr>)}</tbody></table></div></AppShell>}

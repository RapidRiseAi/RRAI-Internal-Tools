import { AppShell } from "@/components/app-shell";
import { KnowledgeBaseForm } from "@/components/forms";
import { Card, PageHeader, StatusBadge } from "@/components/ui";
import { genericList } from "@/lib/data";
import type { KnowledgeBaseItem } from "@/lib/types";
export const dynamic = "force-dynamic";
export default async function Knowledge() { const items = await genericList<KnowledgeBaseItem>("knowledge_base_items"); return <AppShell><PageHeader eyebrow="Procedures" title="Knowledge base" description="Write SOPs, sales scripts, discovery questions, pricing rules, proposal templates, troubleshooting and handover guides." /><div className="grid gap-6"><Card><h2 className="mb-4 text-lg font-semibold">Add SOP / article</h2><KnowledgeBaseForm /></Card><div className="grid gap-4 md:grid-cols-5">{[["Articles", items.length], ["SOPs", items.filter((item) => item.category === "SOP").length], ["Sales", items.filter((item) => item.category === "Sales").length], ["Troubleshooting", items.filter((item) => item.category === "Troubleshooting").length], ["Internal", items.filter((item) => item.visibility === "INTERNAL").length]].map(([label, value]) => <Card key={label}><p className="text-sm text-slate-400">{label}</p><p className="mt-3 text-2xl font-bold text-white">{value}</p></Card>)}</div><Card><div className="grid gap-3">{items.map((item) => <article key={item.id} className="rounded-xl bg-white/[0.04] p-4"><div className="flex items-center justify-between"><h2 className="font-semibold text-white">{item.title}</h2><StatusBadge value={item.visibility} /></div><p className="mt-1 text-xs text-rapid-cyan">{item.category}</p><p className="mt-3 whitespace-pre-wrap text-sm leading-6 text-slate-300">{item.body}</p></article>)}</div></Card></div></AppShell>; }

import { AppShell } from "@/components/app-shell";
import { RetainerForm } from "@/components/forms";
import { Card, PageHeader, StatusBadge } from "@/components/ui";
import { genericList, listClients } from "@/lib/data";
import { dateShort, money } from "@/lib/format";
import type { Retainer } from "@/lib/types";
export const dynamic = "force-dynamic";
export default async function Retainers() { const [retainers, clients] = await Promise.all([genericList<Retainer>("retainers"), listClients()]); return <AppShell><PageHeader eyebrow="Recurring revenue" title="Retainers" description="Track hosting, maintenance, SEO, AI bot, dashboard, portal and automation monitoring retainers." /><div className="grid gap-6"><Card><h2 className="mb-4 text-lg font-semibold">Create retainer</h2><RetainerForm clients={clients} /></Card><div className="grid gap-4 md:grid-cols-5">{[["Retainers", retainers.length], ["Active MRR", money(retainers.filter((retainer) => retainer.status === "ACTIVE").reduce((sum, retainer) => sum + retainer.monthly_amount_cents, 0))], ["Overdue", retainers.filter((retainer) => retainer.status === "OVERDUE").length], ["Paused", retainers.filter((retainer) => retainer.status === "PAUSED").length], ["Upgrade pending", retainers.filter((retainer) => retainer.status === "UPGRADE_PENDING").length]].map(([label, value]) => <Card key={label}><p className="text-sm text-slate-400">{label}</p><p className="mt-3 text-2xl font-bold text-white">{value}</p></Card>)}</div><Card><div className="grid gap-3">{retainers.map((retainer) => <div key={retainer.id} className="flex items-center justify-between rounded-xl bg-white/[0.04] p-3"><div><p className="font-semibold text-white">{retainer.type}</p><p className="text-sm text-slate-400">Next billing {dateShort(retainer.next_billing_date)}</p></div><div className="flex items-center gap-3"><p>{money(retainer.monthly_amount_cents)}</p><StatusBadge value={retainer.status} /></div></div>)}</div></Card></div></AppShell>; }
